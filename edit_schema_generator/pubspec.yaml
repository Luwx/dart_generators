name: edit_schema_generator
description: "Edit lens schema DSL and source generator for lens boilerplate."
version: 0.0.2
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  analyzer: ^12.1.0
  build: ^4.0.6
  collection: ^1.19.1
  source_gen: ^4.2.3

dev_dependencies:
  build_runner: ^2.4.13
  flutter_lints: ^6.0.0
  test: ^1.25.15
