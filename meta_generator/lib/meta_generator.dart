/// A code generator that produces `FooMeta` classes with field and method enums
/// for classes annotated with [@withMeta].
///
/// Works standalone or alongside `@freezed` from the freezed package.
library meta_generator;

export 'src/annotations.dart';
