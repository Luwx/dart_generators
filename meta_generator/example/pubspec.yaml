name: meta_generator_example
description: Example showing meta_generator usage with and without freezed.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  freezed_annotation: ^2.4.0
  json_annotation: ^4.8.0
  meta_generator:
    path: ../

dev_dependencies:
  build_runner: ^2.4.0
  freezed: ^2.4.0
  json_serializable: ^6.7.0
